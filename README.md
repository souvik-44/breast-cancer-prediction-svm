## breast-cancer-prediction-svm
### This project builds a Support Vector Machine (SVM) model to predict malignant vs benign tumors using the Breast Cancer Wisconsin dataset (via sklearn). It includes EDA, feature scaling with StandardScaler, hyperparameter tuning using GridSearchCV (C & gamma), and evaluation using accuracy, confusion matrix, classification report,
